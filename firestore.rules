rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through API routes using Firebase Admin SDK
    // which bypasses these rules. Lock down direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
