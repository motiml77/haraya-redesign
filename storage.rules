rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Audio replies - anyone can read, only authenticated users can write
    match /audio-replies/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
